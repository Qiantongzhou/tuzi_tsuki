@using tuzi_tsuki.Models;
@model songVM2;

@{
    ViewData["Title"] = "批量添加歌";
}

<div class="row">
    <div class="col-md offset">
        <p>
            <h4>歌批量添加</h4>
        </p>

        @if (TempData["Error"] != null)
        {
            <div class="col-md alert alert-danger">
                <span> - @TempData["Error"] </span>
            </div>
        }

        <div class="row">
            <div class="col-md offset">
                <form asp-action="AddSong2">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="Songtext" class="control-label"></label>
                        <input asp-for="Songtext" class="form-control" />
                        <span asp-validation-for="Songtext" class="text-danger"></span>
                    </div>


                    <div class="form-group">
                        <input class="btn btn-outline-success float-right" type="submit" value="添加" />
                        <a class="btn btn-outline-secondary" asp-controller="Movies" asp-action="Index">取消</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>